# 生态世界沙盒 · 项目文档

> 上帝视角的种族生态沙盒：程序化世界自运行（繁衍 / 扩张 / 战争 / 兴衰），玩家用天气、地形、疾病等神力干预进程。
> 本文档用于**沉淀已实现内容**，便于后续提新需求时快速建联定位。

---

## 1. 项目简介

| 项 | 说明 |
|---|---|
| 类型 | 上帝视角生态沙盒 / 程序化模拟 |
| 形态 | 单文件 HTML（Canvas + 原生 JS，无依赖，浏览器直接打开） |
| 主程序 | [`生态世界沙盒.html`](生态世界沙盒.html) |
| 核心循环 | `requestAnimationFrame` 驱动；世界自运行 + 玩家实时干预 |
| 设计原型 | 精灵(森林) / 矮人(山地) / 人马(草原) / 鳄鱼(沼泽) 四族 |

### 玩法一句话
程序化生成地图 → 四族小人按 biome 分布并自主游走 → 出生/死亡的人口循环 → 领地扩张 → 接壤战争；玩家随时用 6 种神力改写进程。

---

## 2. 文件清单（项目资产）

| 文件 | 角色 | 说明 |
|---|---|---|
| [`生态世界沙盒.html`](生态世界沙盒.html) | **主程序（可玩原型）** | 地图+小人+生态+战争+神力，全部集成 |
| [`种族小人形象生成器.html`](种族小人形象生成器.html) | 角色生成器 | 像素小人造型/5 动作/朝向/导出 PNG（完整版大图谱） |
| [`游戏地形生成器.html`](游戏地形生成器.html) | 地形生成器 | biome 地图/河流/4 种地图类型/导出 PNG |
| [`种族生态沙盒-数值推演表.xlsx`](种族生态沙盒-数值推演表.xlsx) | 数值推演 | Logistic 人口 / Lotka-Volterra 食物链 / SIR 疾病 / 四族对比 |

> 主程序里的小人是**迷你简化版**；完整 5 动作大图谱在「种族小人形象生成器」里，二者设计语言一致，未来可互换接入。

---

## 3. 快速上手（怎么玩）

1. 浏览器打开 [`生态世界沙盒.html`](生态世界沙盒.html)
2. 顶部第一排：重建世界 / 地图类型 / 每族数量 / 速度 / 河流 / 领地 / 暂停
3. 顶部第二排「神力 ✋」：选一个神力 → 在地图上**点击或按住拖动**施法（笔刷半径可调）
4. HUD 实时显示总人口 + 各族 `当前/承载力`；左上角弹出瘟疫/战争事件

---

## 4. 核心功能模块（10 大模块）

### 4.1 程序化地图
- **噪声**：值噪声 `vnoise` + 5 层 `fbm`（整数哈希 `ihash`）
- **属性**：海拔 `elev` / 湿度 `moist` / 温度 `temp`（温度 = 1 − 纬度×1.45 − 海拔×0.35）
- **地图类型**（海拔衰减规律）：大陆 / 群岛 / 盆地内海 / 超大陆
- **Biome 映射** `biomeOf(e,m,t)`：14 种地貌（见 §6）
- **河流**：高海拔点沿最陡下降流淌入海
- 渲染到**离屏 `bg` canvas**（每帧复用，性能高）

### 4.2 小人（Agent）
- 按各族 **栖息地 biome**（`HAB`）初始放置
- **AI 游走**：`pickTarget` 在 home 附近随机选同 biome 目标 → 直线移动 → 到达休息 → 换目标
- **迷你像素绘制** `drawAgent`：四族差异化剪影 + 行走腿交替 + 身体起伏 + 朝向翻转 + 状态标记（瘟疫红点/饥饿黄点/交战火花）
- 按 y 排序绘制实现**远近遮挡**

### 4.3 人口动态（每 `ECO_INTERVAL=18` 帧）`ecoTick`
- **繁殖**：Logistic `r_eff = r×(1 − n/K)`，新生儿在父母附近同 biome 出生
- **死亡**：年老（age>maxAge）/ 饥饿（人口过载 → hunger 飙升）/ 瘟疫 / 小概率自然
- **承载力 K**：`KCAP[race] = 栖息地 tile 数 × 0.5`
- **总量上限** 650（防卡顿）

### 4.4 瘟疫 `maybePlague`
- 随机偶发（0.5%/eco）选一族感染一批小人，感染期内额外死亡率
- 也可由玩家神力主动施放

### 4.5 领地与扩张
- `computeTerritory`：小人所在格及周围 1 格标为本族领地（后写覆盖 → 接壤区归后者）
- `renderTerritory`：半透明族色叠加层（可开关）
- `expansion`：人口压力 `n/K > 0.8` 时派「开拓者」去 `findFrontier` 找的未占适宜新地 → 领地蔓延

### 4.6 侵略 / 战争（每 10 帧）`combatTick`
- **空间网格**找邻近（<1.1 格）异族小人，35% 概率交战
- **战力** `STR`：矮人 1.5 > 人马 1.2 > 精灵 1.0 > 鳄鱼 0.8；`战力×(0.5~1.5随机)` 定胜负，败者阵亡
- 头顶火花特效；单轮阵亡≥4 弹消息

### 4.7 玩家神力 `applyPower`（鼠标点击/拖动）
| 神力 `power` | 效果 |
|---|---|
| `rain` 降雨 | 湿度↑ → 变森林/绿洲 |
| `raise` 升地 | 海拔↑ → 造山隔断 |
| `water` 挖水 | 海拔↓ → 造河造海隔离 |
| `forest` 造林 | 湿度↑ + 直接变森林 |
| `plague` 瘟疫 | 范围内小人染病 |
| `meteor` 灾祸 | 范围内小人灭杀 + 地表变火山焦土 |
- `forEachTile` 圆形笔刷（半径 `brushR` 1~6）；改地形后 `recalc` 重算 biome + `renderBg`

### 4.8 建筑/聚落系统
- `checkSettlements`：同格 ≥5 同族小人 + 适宜 biome → 生成 1 级聚落；冷却 12 次 `ecoTick`，同族间距 ≥5 格
- 三级聚落：1级小屋（≥5人）→ 2级中房+旗帜（≥9人）→ 3级城堡+塔楼（≥18人）
- 繁殖加成：聚落 4 格内 `rEff × (1 + 0.12 × level)`
- 战损：异族在聚落 1.6 格内交战获胜 → HP -35~-65；HP≤0 时聚落被摧毁
- `meteor` 神力可直接夷平范围内聚落
- `drawSettlement`：像素建筑绘制 + HP<60 红色受损 overlay

### 4.9 实时人口曲线图
- 位置：地图右侧，300px 宽 × 702px 高（与地图同高）
- 每 `ECO_INTERVAL=18` 帧采样一次各族人口，保存最近 `MAX_HIST=300` 个点
- 四族折线（各族 HUD 色），末端带深色底数值标签
- Y 轴动态计算，最小基准 650，向上取整到 50 的倍数

### 4.10 胜负判定 + 事件流面板
- **左侧事件流**：200px 面板，记录最近 15 条事件，带 `分:秒` 时间戳
- **事件分级**：普通(•) / 警告(⚠) / 里程碑(★) / 结局(◆)，四级颜色区分
- **胜负规则**：单族灭绝只记录事件，不结束游戏；**只剩一个种族**时弹出「🏆 世界统一」结局面板
- **结局面板**：存活帧数 / 剩余人口 / 聚落总数 / 四族最终人口；提供「🔄 再来一局」按钮
- **事件埋点**：聚落建立/升级、人口破百、大规模交战、聚落攻陷、瘟疫、灾祸、统一/灭绝

---

## 5. 技术架构

### 5.1 Agent 数据结构
```js
{
  race,            // 'forest'|'dwarf'|'nomad'|'marsh'
  seed,            // 随机相位（动画/造型）
  x, y,            // tile 浮点坐标
  homeI, homeJ,    // 家园 tile（扩张时迁移）
  tx, ty,          // 当前移动目标
  facing,          // 1 右 / -1 左
  moving, rest,    // 移动状态 / 休息计时
  age, hunger,     // 年龄 / 饥饿值
  plague, fight    // 瘟疫计时 / 交战特效计时
}
```

### 5.2 全局状态
```
W=96, H=54, CELL=13              // 地图格子数 / 每格像素
elev[], moist[], temp[]         // 地块属性（长度 W*H）
biome[], river[]                // biome 字符串 / 河流标记
agents[]                        // 所有小人
terr[]                          // 领地归属（0 无主 / 1-4 族）
KCAP{}                          // 各族承载力
bg, terrCv                      // 离屏画布：地图层 / 领地层
settlements[]                   // 聚落数组（含 level/hp）
popHistory[]                    // 人口历史（用于右侧图表）
eventLog[]                      // 事件流历史（左侧面板）
prevPop{}                       // 上轮人口（用于灭绝检测）
gameOver                        // 胜负已分标记
power, brushR, hoverI/J, painting // 神力交互状态
frame                           // 全局帧计数
```

### 5.3 主循环 `tick()`（rAF）
```
if(!paused){
  frame++
  for agent: fight--；rest 或 朝目标移动（facing 跟随）
  frame % 10  === 0 → combatTick()    // 战争
  frame % 18  === 0 → ecoTick()       // 人口/扩张/领地
}
draw()
```

### 5.4 渲染分层 `draw()`
```
drawImage(bg)            // 地图（离屏缓存）
drawImage(terrCv)        // 领地半透明层（可开关）
for s in settlements: drawSettlement(ctx,s)   // 聚落建筑
agents.sort(y).forEach(drawAgent)   // 小人（含动画/状态）
笔刷光标圆圈              // 神力 hover 预览
drawChart()              // 右侧人口曲线图（仅在 ecoTick 后重绘）
```

---

## 6. 关键参数速查表

### 种族参数
| 种族 | key | 栖息地 HAB | 繁殖 r | 寿命 maxAge | 战力 STR | 迷你色 |
|---|---|---|---|---|---|---|
| 精灵(森林) | forest | FOREST, PLAIN | 0.030 | 2600 | 1.0 | #5bbf5a |
| 矮人 | dwarf | MOUNT, HILL | 0.016 | 3200 | **1.5** | #caa36a |
| 人马(游牧) | nomad | GRASS, PLAIN | 0.022 | 2400 | 1.2 | #cf8a4a |
| 鳄鱼(沼泽) | marsh | SWAMP, BEACH | **0.040** | 2000 | 0.8 | #3fa07a |

### Biome 映射 `biomeOf(e,m,t)`（e=海拔 m=湿度 t=温度）
```
e<0.34 → DEEPSEA      e<0.40 → SEA        e<0.435 → BEACH
e>0.90 → SNOW/ROCK(t<0.32)   e>0.78 → MOUNT     e>0.70 → HILL
t<0.26 → SNOW
e<0.50 且 m>0.66 → SWAMP
m<0.24 → DESERT   m<0.46 → GRASS   m<0.68 → PLAIN   否则 → FOREST
```

### 地图类型（海拔衰减，d=离中心归一距离）
| 类型 | 公式 |
|---|---|
| continent 大陆 | e − d×0.55 + 0.12 |
| islands 群岛 | e×1.15 − 0.22 |
| basin 盆地 | e + (d−0.5)×0.55 |
| pangaea 超大陆 | e − d×0.30 + 0.18 |

### 关键时序 / 阈值
| 参数 | 值 | 含义 |
|---|---|---|
| ECO_INTERVAL | 18 帧 | 人口动态周期 |
| 战斗检测 | 每 10 帧 | combatTick |
| 战斗范围 / 概率 | <1.1 格 / 35% | 接壤交战 |
| 承载力系数 | ×0.5 | KCAP = 栖息地 tile × 0.5 |
| 扩张阈值 | n/K > 0.8 | 触发开拓者 |
| 总人口上限 | 650 | 防卡顿 |
| 笔刷半径 | 1~6 | brushR |
| 聚落生成人口 | ≥5 同族/格 | 触发新聚落 |
| 聚落生成冷却 | 12 次 ecoTick | 约 216 帧 ≈ 3.6s |
| 聚落最小间距 | 5 格 | 同族聚落之间 |
| 聚落繁殖加成 | +12%/lv | 1级+12%，3级+36% |
| 聚落战损 | 35~65 HP/次 | 异族交战获胜 |
| 图表历史容量 | 300 点 | 约 90 秒 @60fps |
| 事件流容量 | 15 条 | 左侧面板滚动 |
| 统一胜利条件 | 只剩 1 个种族 | 其他三族全部灭绝 |

---

## 7. 代码结构索引（函数 → 职责）

| 分组 | 函数 | 职责 |
|---|---|---|
| 噪声 | `ihash` `vnoise` `fbm` | 整数哈希值噪声 + 分形叠加 |
| 地图 | `biomeOf` | 属性→biome 映射 |
| | `generate` | 生成 elev/moist/temp/biome/river |
| | `renderBg` / `shade` | 地图渲染到离屏 / 海拔明暗 |
| 小人 | `newAgent` `initAgents` | 创建/初始化小人 |
| | `pickTarget` | AI 选目标 |
| | `drawAgent` | 迷你像素小人绘制（含动画/状态） |
| 人口 | `ecoTick` | 繁殖/死亡总调度 |
| | `spawnNear` `maybePlague` | 生育/瘟疫 |
| | `updateHud` `flashMsg` | HUD / 事件提示 |
| 领地 | `computeTerritory` `renderTerritory` | 计算/渲染领地 |
| | `findFrontier` `expansion` | 找新地/派开拓者 |
| 战争 | `combatTick`（+`STR`） | 接壤交战结算 |
| 聚落 | `checkSettlements` `cleanupSettlements` | 生成/清理聚落 |
| | `drawSettlement` | 聚落像素绘制 |
| 图表 | `drawChart` | 右侧人口折线图绘制 |
| 事件 | `pushEvent` `renderEventLog` | 压入事件/渲染面板 |
| | `checkVictory` `showGameOver` | 胜负判定/结局面板 |
| 神力 | `recalc` `forEachTile` `applyPower` | 地块重算/笔刷/施法 |
| 循环 | `draw` `tick` | 渲染分层 / 主循环 |
| 入口 | 末尾事件绑定 | 神力工具栏 + 鼠标事件 + `rebuild()` `tick()` |

---

## 8. 数值模型对应（理论 → 代码）

| 数值模型（见 xlsx） | 代码实现 |
|---|---|
| Logistic 人口增长 | `ecoTick` 繁殖 `r×(1−n/K)` |
| 环境容量 K | `KCAP` = 栖息地 tile × 0.5 |
| SIR 疾病 | `plague` 计时 + 死亡率（简化版） |
| 压力-扩张 | `expansion` 的 `n/K>0.8` |
| Lanchester 战斗 | `combatTick` 的 `STR` 战力对比（简化版） |
| 温度对繁殖 M_temp | （暂未接入，见 §9 扩展点） |

---

## 9. 已实现 vs 可扩展

### ✅ 已实现
- [x] 程序化地图（14 biome / 河流 / 4 地图类型）
- [x] 四族小人（迷你造型 / 行走动画 / 朝向 / 状态标记）
- [x] 按 biome 分布 + 自主游走 AI
- [x] 人口动态（繁殖 / 饥饿 / 年老 / 瘟疫）
- [x] 领地扩张（领地可视化 + 开拓者）
- [x] 侵略战争（空间网格 + 战力系统）
- [x] 玩家神力（降雨/升地/挖水/造林/瘟疫/灾祸）
- [x] 建筑/聚落系统（三级聚落 / 繁殖加成 / 战损摧毁）
- [x] 实时人口曲线图（右侧折线 / 300点历史 / 动态Y轴）
- [x] 胜负判定（灭绝制胜利 / 中央结局面板 / 再来一局）
- [x] 事件流面板（左侧15条 / 四级事件分级 / 全埋点）

### ⏳ 待扩展（常见下一步）
- [ ] 接入完整 5 动作大图谱小人（替换迷你版）
- [ ] 温度/食物链对繁殖的影响（接入 M_temp、Lotka-Volterra）
- [ ] 存档/分享（导出 seed + 状态）
- [ ] 季节/天气周期（全局气候循环）
- [ ] 音效（事件分级预埋 Audio()）
- [ ] 建筑/科技/外交等深度系统
- [ ] 接入完整 5 动作大图谱小人（替换迷你版）
- [ ] 打包为 Godot/Unity 可发布版本

---

## 10. 如何提新需求（建联指引）⭐

> 未来提需求时，**附上本文档路径**并说明诉求，即可快速定位到要改的模块/函数。

### 提需求模板
```
参考《生态世界沙盒-项目文档.md》，我想给 [生态世界沙盒.html] 新增/修改：
- 诉求：______（如"小人会建房子"、"加食物资源"、"季节变化"）
- 期望表现：______
```

### 常见需求 → 改动定位速查
| 你想做的事 | 主要改动位置 |
|---|---|
| 调种族强弱/繁殖/寿命 | `RACE_PARAMS` / `STR` / `HAB` |
| 改地图地貌/海陆比例 | `biomeOf` 阈值 / 地图类型衰减 |
| 改小人长相/动画 | `drawAgent`（或接入大图谱） |
| 改人口/死亡规则 | `ecoTick` |
| 改战争规则 | `combatTick` / `STR` |
| 改领地/扩张逻辑 | `computeTerritory` / `expansion` / `findFrontier` |
| 新增一种神力 | `applyPower` 加分支 + 工具栏加按钮 |
| 加新种族 | `HAB`/`RACE_NAME`/`RACE_PARAMS`/`STR`/`RACE_IDX`/`TERR_COL`/`drawAgent` 同步加 |
| 加 HUD/图表/面板 | `updateHud` + HTML 容器 |
| 加聚落/建筑逻辑 | `checkSettlements` / `drawSettlement` / 升级判定 |
| 加图表/曲线 | `drawChart` + `popHistory` 采样 |
| 加事件流/胜负 | `pushEvent` / `checkVictory` / `showGameOver` |
| 加新地块属性（如资源） | 新增全局数组 + `generate` 填充 + `ecoTick` 消费 |

### 接入新种族的完整清单（避免遗漏）
新增一族需同步更新：`HAB`、`RACE_NAME`、`RACE_PARAMS`、`STR`、`RACE_IDX`、`TERR_COL`、`RACE_COL`、`drawAgent` 的 switch 分支、HUD pop 统计对象。

### 技术约束备忘
- 单文件、无依赖、原生 Canvas；保持这一形态便于直接打开调试
- 性能敏感处：`combatTick`（空间网格已优化）、`renderBg`（仅地形变更时重渲染）
- 坐标体系：小人用 **tile 浮点坐标**，绘制时 ×`CELL`；鼠标用 `getBoundingClientRect` 换算（适配缩放）

---

---

## 11. 今日新增内容速查（2026-06-06）

> 以下内容为快速索引，与 §4 一一对应。

### 11.1 聚落系统关键函数

| 函数 | 调用位置 | 职责 |
|---|---|---|
| `checkSettlements()` | `ecoTick` 末尾 | 检测生成新聚落 + 动态升级 |
| `cleanupSettlements()` | `ecoTick` 末尾 | 过滤 HP≤0 的聚落 |
| `drawSettlement(ctx,s)` | `draw()` 中 agents 之前 | 绘制三级像素建筑 + 受损特效 |

### 11.2 图表系统关键函数

| 函数 | 调用位置 | 职责 |
|---|---|---|
| `drawChart()` | `ecoTick` 末尾 | 绘制右侧人口折线图 |

### 11.3 事件与胜负关键函数

| 函数 | 调用位置 | 职责 |
|---|---|---|
| `pushEvent(type,text)` | 各事件埋点 | 压入事件并刷新左侧面板 |
| `renderEventLog()` | `pushEvent` 内部 | 渲染 `#eventList` |
| `checkVictory()` | `ecoTick` 末尾 | 检测灭绝/统一/里程碑 |
| `showGameOver(race,pop)` | `checkVictory` 内 | 弹出中央结局面板 + 暂停 |

### 11.4 事件埋点速查

| 埋点位置 | 事件内容 | 级别 |
|---|---|---|
| `checkSettlements` — 建立 | XX 建立了新聚落 | 普通 |
| `checkSettlements` — 升级 | XX 聚落升至 2/3 级 | 里程碑 |
| `ecoTick` | XX 人口突破 100 | 里程碑 |
| `combatTick` — 大规模交战 | 领地交战，N 人阵亡 | 警告 |
| `combatTick` — 聚落战损 | XX 的聚落被攻陷 | 警告 |
| `maybePlague` | XX 爆发瘟疫，感染 N 人 | 警告 |
| `applyPower(meteor)` — 灭人 | 天降灾祸，N 人罹难 | 警告 |
| `applyPower(meteor)` — 夷平聚落 | XX 的聚落被灾祸夷平 | 警告 |
| `checkVictory` — 灭绝 | XX 已灭绝 | 结局（仅记录） |
| `checkVictory` — 统一 | XX 统一了世界！ | 结局（弹窗） |

### 11.5 技术约束更新
- 单文件、无依赖、原生 Canvas 形态保持不变
- 新增性能敏感处：`drawChart()` 仅在 `ecoTick` 时重绘（每 18 帧），不每帧绘制
- 布局采用三栏 flex：左侧事件流(200px) + 中间地图(1248px) + 右侧图表(300px)

---

---

## 12. 更新日志（2026-06-06）

> 以下为本日新增/修改内容，与 §4 / §5 / §6 / §7 一一对应。

### 12.1 领地边界线

- 在 `renderTerritory()` 中**新增边界线绘制**：每个领地格子与无主地/异族地的交界处，绘制 2px 粗、90% 透明度的实色边界线；同族内部相邻边不绘制，避免杂乱。
- 保留原有的 30% 透明度填充，确保底层地貌可见。

### 12.2 领地系统重构（持久化）

将原 `computeTerritory()`（每帧 `terr.fill(0)` 清空后重算）重构为**持久化领地系统**：

| 变更项 | 说明 |
|---|---|
| **实时标记** | `tick()` 每帧小人移动后，立即将小人所在格及周围 1 格标记为本族领地（覆盖异族） |
| **持久化** | `terr` 数组不再每帧清空，走过的地方**永久保留** |
| **覆盖机制** | 异族小人经过时直接覆盖领地归属（后写覆盖） |
| **无主化规则** | 新增 `decayTerritory()`：每 `ECO_INTERVAL`（18 帧）检测一次；若某领地格子**无同族小人**且**上/下/左/右四邻居都不是同族领地**，则该格子置为无主（0） |
| **初始化** | `initAgents()` 中 `terr.fill(0)` 清空领地，由小人从脚下实时重新生长 |

### 12.3 种族颜色优化

将四大种族颜色更新为**更高饱和度、与自然地貌区分度更强**的配色，同步更新 `TERR_COL`、`RACE_COL`、`drawAgent`（小人像素）、`buildLegend`（图例）：

| 种族 | 旧颜色 | 新颜色 | 说明 |
|---|---|---|---|
| 精灵 | `#5bbf5a` | `#00e676` 亮翠绿 | 比森林绿更亮更偏青，在自然背景中非常跳脱 |
| 矮人 | `#caa36a` | `#ff9100` 亮橙 | 类似熔岩/琥珀，与山地灰、沙漠黄明显区分 |
| 人马 | `#cf8a4a` | `#ff1744` 亮玫红 | 与所有自然地貌强烈对比，极具侵略感 |
| 鳄鱼 | `#3fa07a` | `#00b0ff` 亮青蓝 | 与沼泽深绿、水域蓝区分明显，冷色调突出 |

### 12.4 代码结构更新

| 分组 | 函数 | 职责变更 |
|---|---|---|
| 领地 | `computeTerritory()` → **`decayTerritory()`** | 由"每帧清空重算"改为"无主化衰减检测" |
| 领地 | `tick()` 内小人循环 | 新增实时领地标记逻辑（3×3 笔刷覆盖） |
| 入口 | `initAgents()` | 末尾改为 `terr.fill(0); renderTerritory();` |

### 12.5 技术约束更新
- 单文件、无依赖、原生 Canvas 形态保持不变
- `terr` 数组持久化后内存占用不变（仍为 `W*H` 整数数组）
- `decayTerritory()` 每 18 帧遍历 5184 格 + Set 查询，性能开销可忽略

---

_最后更新：2026-06-06，随主程序 [`生态世界沙盒.html`](生态世界沙盒.html) 当前实现。_
